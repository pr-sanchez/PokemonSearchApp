import React from 'react';
import ReactDOM from 'react-dom';
import PokemonContext from './components/page/PokemonContext';

ReactDOM.render(
  <React.StrictMode>
    <PokemonContext />
  </React.StrictMode>,
  document.getElementById('root')
);